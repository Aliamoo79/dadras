# پرامپت آماده برای ساخت دموی «سیستم هوش مصنوعی قضایی»

این پرامپت را می‌توانید مستقیم به یک ابزار کدنویسی هوش مصنوعی (مثل Claude Code، ChatGPT، Cursor و...) بدهید تا یک دموی کاربردی از پروژه بسازد. جاهایی که با `[...]` مشخص شده را قبل از ارسال با جزئیات پروژه‌ی خودتان پر کنید.

---

## پرامپت

من می‌خواهم یک **دموی کاربردی (Proof of Concept)** از یک سیستم چندعامله (Multi-Agent) هوش مصنوعی برای پردازش پرونده‌های قضایی بسازی. هدف دمو، نمایش گردش کار کامل از ورود پرونده تا صدور رای پیشنهادی است، نه یک سیستم قضایی واقعی و عملیاتی.

### ۱. زمینه و هدف پروژه
سیستم قرار است در دادگاه‌ها به قضات کمک کند تا:
- پرونده‌های طولانی را خلاصه کند و صورت‌جلسه تنظیم کند
- قوانین و مواد قانونی مرتبط با هر پرونده را پیدا کند (RAG)
- نوع و اهمیت پرونده را تشخیص دهد و به شعبه مناسب ارجاع دهد
- پیش‌نویس رای اولیه، رای تجدیدنظر و در صورت نیاز رای دیوان عالی را تولید کند
- صحت استنادهای قانونی خروجی را راستی‌آزمایی کند
- به سوالات عمومی شهروندان به‌صورت ۲۴ ساعته پاسخ دهد

نمونه‌های الهام‌بخش: دادگاه هوشمند هانگژو (چین)، نرم‌افزار COMPAS (آمریکا)، ربات قاضی استونی.

### ۲. معماری مورد نظر (لایه‌ها)
لطفاً دمو را دقیقاً بر اساس این معماری پنج‌لایه پیاده‌سازی کن:

1. **لایه ادراک (Perception):** دریافت فایل ورودی (PDF یا فایل صوتی)، تبدیل به متن (OCR/ASR شبیه‌سازی‌شده یا واقعی)، تشخیص موجودیت‌ها (اشخاص، نقش‌ها، کد ملی)، ذخیره در دیتابیس.
2. **لایه پردازش (Processing):** فهم متن با LLM + بازیابی قوانین مرتبط با روش RAG از یک پایگاه دانش قانونی (Vector DB).
3. **لایه استدلال (Reasoning) — چند عامل موازی/ترتیبی:**
   - عامل تشخیص اشخاص (شاکی، متهم، وکیل، قاضی)
   - عامل تشخیص ارجاعات قانونی (استخراج مواد و تبصره‌ها)
   - عامل طبقه‌بندی پرونده (کیفری/حقوقی/تجاری + درجه اهمیت)
   - عامل(های) وکیل شاکی و متهم
   - عامل(های) کارشناس (مالی، فنی) برای تعیین خسارت/ارزش اموال
   - عامل قاضی بدوی (صدور رای اولیه)
   - عامل قاضی تجدیدنظر (صدور رای قطعی)
   - عامل قاضی دیوان عالی (در صورت نقض، ارسال برای رسیدگی مجدد)
   - عامل بازرس
4. **لایه تصمیم‌گیری نهایی:** عامل تلفیق‌کننده که گزارش‌های همه عامل‌ها را جمع‌بندی می‌کند.
5. **لایه صحت‌سنجی (Validation):** عامل بررسی اعتبار مواد قانونی استنادشده در رای نهایی، تا مطمئن شویم قانون واقعاً وجود دارد و به‌روز است.

جریان کلی که باید در دمو دیده شود:
`فایل PDF/صوتی → تبدیل به متن → [طبقه‌بندی پرونده + تشخیص اشخاص + استخراج قوانین] → بازیابی قوانین مرتبط (RAG) → عامل‌های کارشناس → عامل وکیل شاکی/متهم → عامل قاضی بدوی → عامل قاضی تجدیدنظر → عامل قاضی دیوان عالی (در صورت نیاز) → عامل صحت‌سنجی رای نهایی`

### ۳. نیازمندی‌های فنی
- **زبان و رابط کاربری:** فارسی و راست‌به‌چپ (RTL)، فونت خوانا برای فارسی.
- **بک‌اند:** [ترجیح شما، مثلاً Python + FastAPI]
- **هماهنگی چندعامله:** [ترجیح شما، مثلاً LangGraph یا یک state machine ساده دست‌ساز]
- **RAG:** یک پایگاه دانش کوچک نمونه از چند ماده قانونی ساختگی/نمونه (چون داده واقعی نداریم) + Embedding + Vector DB (مثلاً Chroma یا FAISS)
- **فرانت‌اند:** یک صفحه ساده وب که:
  - آپلود فایل پرونده را نشان دهد
  - مراحل پردازش را به‌صورت زنده و گام‌به‌گام نمایش دهد (مثل یک تایم‌لاین یا نمودار جریان که هر عامل روشن می‌شود)
  - خروجی هر عامل (خلاصه، قوانین استخراج‌شده، استدلال هر طرف، رای پیشنهادی) را جداگانه و قابل کلیک نشان دهد
  - در پایان، رای نهایی به همراه فهرست مواد قانونی مورد استناد و نتیجه صحت‌سنجی را نمایش دهد
- **داده نمونه:** چون داده واقعی پرونده نداریم، ۱ تا ۲ پرونده نمونه ساختگی (مثلاً یک دعوای مالی ساده) طراحی کن تا در دمو قابل استفاده باشد.

### ۴. محدودیت‌ها و نکات ایمنی
- این یک **دمو آموزشی/نمایشی** است، نه ابزار واقعی صدور رای؛ لطفاً یک بنر یا هشدار واضح در رابط کاربری بگذار که خروجی صرفاً پیشنهادی است و باید توسط قاضی انسانی تایید شود.
- شفافیت (Explainability) مهم است: هر عامل باید دلیل تصمیمش و مواد قانونی مورد استناد را نشان دهد، نه فقط نتیجه نهایی.
- در صورت نبود API واقعی برای ASR/OCR، از یک نسخه شبیه‌سازی‌شده (mock) استفاده کن اما ساختار کد را طوری بنویس که بعداً به‌راحتی به یک سرویس واقعی وصل شود.

### ۵. خروجی مورد انتظار از تو (هوش مصنوعی)
لطفاً به ترتیب زیر پیش برو:
1. یک نمودار معماری (متنی یا Mermaid) از کل جریان کار ارائه بده.
2. ساختار پوشه‌ها و فایل‌های پروژه را مشخص کن.
3. کد هر لایه/عامل را به‌صورت مرحله‌به‌مرحله بنویس (شروع از لایه ادراک تا صحت‌سنجی).
4. یک رابط کاربری وب ساده اما قابل نمایش برای ذینفعان (مثل قضات یا مدیران دادگستری) بساز.
5. یک پرونده نمونه بساز و کل جریان را روی آن اجرا کن تا خروجی واقعی نمایش داده شود.
6. در پایان یک README کوتاه بنویس که نحوه اجرای دمو را توضیح دهد.

اگر جایی نیاز به تصمیم فنی داری که در بالا مشخص نکرده‌ام (مثلاً انتخاب مدل LLM یا کتابخانه)، منطقی‌ترین گزینه را انتخاب کن و دلیلش را کوتاه توضیح بده، به‌جای اینکه برای هر مورد از من سوال بپرسی.

---

### نکات قبل از استفاده
- جاهای `[...]` را با تکنولوژی‌های موردنظر خودتان (مثلاً چه زبان برنامه‌نویسی، چه فریمورک) پر کنید تا خروجی دقیق‌تر شود.
- اگر می‌خواهید دمو را با Claude Code یا هر ابزار agentic بسازید، همین متن را مستقیم کپی کنید؛ برای ابزارهایی مثل ChatGPT وب هم قابل استفاده است اما نتیجه کدنویسی محدودتر خواهد بود.
- اگر هدف دمو ارائه به سرمایه‌گذار/استاد است نه پیاده‌سازی واقعی، می‌توانید در بخش ۳ درخواست کنید که به‌جای بک‌اند واقعی، یک نسخه کاملاً front-end با داده‌های از پیش تولیدشده (canned responses) ساخته شود تا سریع‌تر و بدون هزینه API آماده شود.
